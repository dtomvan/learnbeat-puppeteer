export const timeouts = {
    MAX_TIMEOUT: Number.MAX_SAFE_INTEGER,
    DISABLE_TIMEOUT: 0
};

export const somOauth = {
    ORG_SEARCH_FIELD_SELECTOR: "#organisatieSearchField",
    USERNAME_FIELD_SELECTOR: "input[name='usernameFieldPanel:usernameFieldPanel_body:usernameField']",
    PASSWORD_FIELD_SELECTOR: "input[name='passwordFieldPanel:passwordFieldPanel_body:passwordField']"
};
